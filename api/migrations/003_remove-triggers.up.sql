drop trigger if exists company_has_same_tid_tg on  core.deed cascade;
drop trigger if exists entry_type_company_has_same_tid_tg on  core.entry cascade;
drop trigger if exists deed_entry_has_same_tid_tg on  core.drain cascade;
